# BeautyOS Next.js Application Audit

Generated by BLACKBOXAI

> Audit scope: `beautyos/` (Next.js App Router). Findings are based on static code inspection of the files available in this workspace.

---

## 1) FILE STRUCTURE AUDIT

### 1.1 `src/app` directory (pages + API routes)

#### Pages / route segments
- `src/app/layout.tsx`
- `src/app/page.tsx`
- `(auth)/` *(route group exists; contents not inspected)*
- `src/app/booking/page.tsx`
- `src/app/bridal/page.tsx`
- `src/app/dashboard/page.tsx`
- `src/app/dashboard/profile/page.tsx`
- `src/app/inspiration/page.tsx`
- `src/app/mens/page.tsx`
- `src/app/planner/page.tsx`
- `src/app/salon-dashboard/page.tsx`
- `src/app/salons/page.tsx`
- `src/app/salons/[id]/page.tsx`
- `src/app/stylist-dashboard/page.tsx`
- `src/app/stylists/page.tsx`
- `src/app/stylists/[id]/page.tsx`

#### API routes
- `src/app/api/ai/chat/route.ts` *(POST)*
- `src/app/api/ai/inspiration/route.ts` *(POST)*
- `src/app/api/ai/planner/route.ts` *(POST)*
- `src/app/api/auth/[...nextauth]/route.ts` *(GET/POST via NextAuth handler)*
- `src/app/api/auth/register/route.ts` *(POST)*
- `src/app/api/bookings/route.ts` *(GET/POST)*
- `src/app/api/salons/route.ts` *(GET)*
- `src/app/api/salons/[id]/route.ts` *(GET)*
- `src/app/api/stylists/route.ts` *(GET)*
- `src/app/api/stylists/[id]/route.ts` *(GET)*
- `src/app/api/stylists/Unconfirmed 60931.crdownload` *(stray/crdownload artifact; remove)*

### 1.2 `src/components` directory
- `src/components/ai-chatbot.tsx`
- `src/components/map-view.tsx`
- `src/components/navbar.tsx`
- `src/components/providers.tsx`
- `src/components/salon-card.tsx`
- `src/components/stylist-card.tsx`

ShadCN UI components:
- `src/components/ui/avatar.tsx`
- `src/components/ui/badge.tsx`
- `src/components/ui/button.tsx`
- `src/components/ui/card.tsx`
- `src/components/ui/dialog.tsx`
- `src/components/ui/fade-in.tsx`
- `src/components/ui/input.tsx`
- `src/components/ui/label.tsx`
- `src/components/ui/radio-group.tsx`
- `src/components/ui/select.tsx`
- `src/components/ui/separator.tsx`
- `src/components/ui/sheet.tsx`
- `src/components/ui/sonner.tsx`
- `src/components/ui/tabs.tsx`
- `src/components/ui/textarea.tsx`

### 1.3 `src/lib` directory
- `src/lib/ai.ts`
- `src/lib/auth.ts`
- `src/lib/db.ts`
- `src/lib/utils.ts`

### 1.4 Missing critical files
1. **`/login` page**: `src/lib/auth.ts` config uses `pages: { signIn: "/login" }` and middleware redirects to `/login`, but no `src/app/login/page.tsx` is present in the file listing.
2. **Potential missing Tailwind/Leaflet config**: `map-view.tsx` imports `leaflet/dist/leaflet.css` (fine), but Leaflet sometimes needs icon asset handling (not audited here).

---

## 2) CODE ANALYSIS FOR EACH FILE (major audited files)

### 2.1 `src/app/layout.tsx`
**What it does**: Root layout; wraps app in `Providers`, renders `Navbar`, wraps children in `<main>`, and renders floating `AIChatbot`.

**Imports**:
- `Navbar`, `AIChatbot`, `Providers`
- `next/font/google` (`Inter`)
- `./globals.css`

**Exports**:
- `metadata`
- default `RootLayout`

**Issues / risks**:
- `suppressHydrationWarning` on `<body>` can hide hydration mismatches.
- `Navbar` + `AIChatbot` render for all routes (client load cost).

### 2.2 `src/app/page.tsx`
**What it does**: Marketing/landing page with links to major features.

**Imports**: `next/link`, UI components, lucide icons, `FadeIn`.

**Issues / risks**: None obvious.

### 2.3 `src/app/salons/page.tsx`
**What it does**: Fetches `/api/salons`, supports search, sort, and list vs map view.

**Imports**: `MapView`, `SalonCard`, shadcn UI.

**Issues / risks**:
- Sort by price uses **string length** (`priceRange.length`) instead of numeric pricing semantics.
- `processedSalons` computed even when `loading` is true (safe due to `salons` default `[]`, but pattern can cause UX flicker).

### 2.4 `src/app/salons/[id]/page.tsx`
**What it does**: Fetches `/api/salons/:id`, shows details and booking CTA.

**Issues / risks**:
- Uses `useParams()` and checks `if (params.id)`—reasonable.
- Trusts response shape (would break with real DB nulls).

### 2.5 `src/app/stylists/page.tsx`
**What it does**: Fetches `/api/stylists`, search.

**Issues / risks**:
- Assumes `stylist.name` and `stylist.specialty` are defined.

### 2.6 `src/app/stylists/[id]/page.tsx`
**What it does**: Fetches `/api/stylists/:id`, shows portfolio + booking.

**Issues / risks**:
- Trusts response shape.

### 2.7 `src/components/navbar.tsx`
**What it does**: Responsive navigation, toggles dashboard links based on `useSession()`.

**Imports**:
- `useSession` + `signOut` from `next-auth/react`
- ShadCN `Sheet`
- UI `Button` + lucide icons

**Issues / risks (important)**:
- **Mobile dashboard link bug**: when `session` exists, the mobile menu always links to `/dashboard` rather than role-specific `/{role}-dashboard` routes.

### 2.8 `src/components/ai-chatbot.tsx`
**What it does**: Floating chat widget; posts user input to `/api/ai/chat`.

**Issues / risks**:
- Assumes API returns `{ response: string }`.
- Does not handle non-200 responses (no `res.ok` check).

### 2.9 `src/components/map-view.tsx`
**What it does**: Leaflet map with markers.

**Issues / risks**:
- Uses `react-leaflet` client component pattern; ok.
- No clustering; fine for small dataset.

### 2.10 `src/lib/auth.ts`
**What it does**: NextAuth credentials provider; JWT session strategy; injects `role` into token and session.

**Exports**:
- `authOptions`

**Issues / risks (critical)**:
- `pages: { signIn: "/login" }` but no `/login` route exists in the audited file list.
- Type augmentation risk: code writes `session.user.role`, but NextAuth default types don’t include `role` unless you have module augmentation (there is `src/types/next-auth.d.ts`, but its contents were not audited here).

### 2.11 `src/lib/db.ts`
**What it does**: Prisma singleton.

**Issues / risks**: None obvious.

### 2.12 `src/lib/ai.ts`
**What it does**: Builds prompts and calls `FREE_LLM_ENDPOINT`.

**Exports**:
- `generateBeautyPlan(prompt, type)`
- `analyzeInspiration(description)`

**Issues / risks (critical)**:
1. **Response parsing mismatch**:
   - Prompts instruct: “Return ONLY a valid JSON object …”
   - But the function returns `data.response`.
   - The code expects the LLM gateway to wrap the JSON in `{ response: ... }`. If the gateway returns raw JSON, parsing will fail or UI will receive a string.
2. **No validation / JSON parse**:
   - Returns `data.response` directly; planner/inspiration route may return unusable content.
3. Uses non-null env vars with `!` and will crash if missing.

---

## 3) BROKEN CONNECTIONS DETECTION

### 3.1 Frontend calls vs API existence
**Confirmed**:
- Frontend salons list/detail match:
  - `GET /api/salons` ✅
  - `GET /api/salons/:id` ✅
- Frontend stylists list/detail match:
  - `GET /api/stylists` ✅
  - `GET /api/stylists/:id` ✅
- Chatbot calls:
  - `POST /api/ai/chat` ✅

**Not fully audited** (pages not inspected in this run):
- Booking page `/api/bookings` (exists)
- Planner page `/api/ai/planner` (exists)
- Inspiration page `/api/ai/inspiration` (exists)

### 3.2 Dynamic route coverage
- `[id]` pages exist for salons and stylists.
- Corresponding `[id]` API endpoints exist.

### 3.3 Middleware configuration
- Middleware is in `src/middleware.ts` (not `src/app/middleware.ts`). In Next.js, this is correct placement.
- Matcher protects dashboards and booking.

**Risk**: If `/login` route is missing, redirects will break navigation.

---

## 4) ERROR DETECTION (static)

### 4.1 TypeScript strict risks
- `session.user.role` assignments in `src/lib/auth.ts` may fail without module augmentation.
- Client pages trust API response shape.

### 4.2 Undefined / null risks
- Filtering/sorting uses fields that may be undefined with real DB data.

### 4.3 Hydration mismatch risk
- `suppressHydrationWarning` in `layout.tsx` may mask issues.
- Navbar uses `useSession()` client-side.

---

## 5) AUTHENTICATION CHECK

### 5.1 NextAuth configuration
- Credentials provider + bcrypt compare ✅
- JWT strategy ✅
- Role injected into token/session ✅ (but type augmentation risk)

### 5.2 Session propagation
- `src/components/providers.tsx` uses `SessionProvider` ✅
- Navbar consumes `useSession()` ✅

### 5.3 User data access correctness
- Booking API uses `session?.user?.email || "guest-user"` as `userId`, but Prisma schema expects `Booking.userId` to reference `User.id`.

---

## 6) AI FEATURES CHECK

### 6.1 `src/lib/ai.ts` prompt issues
- Prompt expects model to return JSON.
- Code expects model response wrapped in `data.response`.
- **Likely broken** integration depending on the LLM gateway response format.

### 6.2 API endpoints for AI planner/inspiration
- Routes exist:
  - `/api/ai/planner` returns `{ plan }` (but `plan` is likely a string or wrong shape)
  - `/api/ai/inspiration` returns `{ analysis }` (analysis likely string)

### 6.3 AI response parsing
- No `JSON.parse` shown in `src/lib/ai.ts`.
- Frontend likely expects parsed objects for timeline/services; mismatch risk is high.

---

## 7) COMPONENT ISSUES

### 7.1 ShadCN components
- UI folder includes expected components.

### 7.2 `map-view.tsx`
- Uses Leaflet; ok.

### 7.3 Navbar mobile menu
- **Bug**: mobile dashboard link ignores user role.

---

## 8) DATABASE / PRISMA CHECK

### 8.1 Prisma schema
- `prisma/schema.prisma` exists and defines models: `User`, `Salon`, `Stylist`, `Service`, `Booking`, etc.

### 8.2 Database connection
- `src/lib/db.ts` creates PrismaClient with SQLite datasource.

### 8.3 Data fetching vs mocks
- API routes for salons/stylists are hardcoded mocks.
- Booking endpoint uses mock bookings and does not persist.
- Therefore the app likely runs without real DB data for these features.

---

## 9) DEPLOYMENT READINESS

### 9.1 Environment variables
- Required by code:
  - `FREE_LLM_ENDPOINT`
  - `FREE_LLM_API_KEY`
  - `DATABASE_URL` (Prisma)

### 9.2 `next.config.ts`
- `output: 'standalone'` ✅
- Remote image patterns includes `images.unsplash.com` ✅

### 9.3 Dockerfile
- Multi-stage build with standalone output ✅
- Copies `public`, standalone `.next/standalone`, `.next/static`

**Risk**: Docker image may fail if `.env` variables are not provided at runtime.

---

## 10) SUMMARY OF TOP 10 CRITICAL ISSUES

1. **Missing `/login` route** (middleware + NextAuth signIn page redirect to `/login`).
2. **AI integration likely broken due to response shape mismatch**: prompts request raw JSON, code returns `data.response` without parsing.
3. **AI planner/inspiration routes likely return wrong data shape** to frontend (planner expects object with `timeline`/`recommendedServices`, but returns string/unknown).
4. **Booking API uses wrong `userId`** (`email` instead of Prisma `User.id`). Even with DB, this breaks relations.
5. **Booking API does not persist** (commented-out Prisma create/find); returns mock data.
6. **Type augmentation risk in NextAuth**: writing `session.user.role` may fail under strict TS without module augmentation.
7. **Navbar mobile dashboard link bug**: always uses `/dashboard` instead of role-based dashboards.
8. **Stray artifact**: `src/app/api/stylists/Unconfirmed 60931.crdownload` should be removed; may confuse deployments/build.
9. **No API error handling on client** (chatbot and pages don’t check `res.ok` / handle non-200).
10. **Price sorting logic is semantically wrong** (uses `priceRange.length`).

---

## Appendix: quick remediation checklist (non-exhaustive)
- Add `src/app/login/page.tsx` (or update auth config/middleware to existing route).
- Fix LLM response parsing:
  - make gateway return raw JSON OR parse `data.response` correctly OR update prompts to match gateway format.
- Update booking API to use `session.user.id` (or look up user by email) and persist via Prisma.
- Add NextAuth module augmentation for `Session.user.role`.
- Fix Navbar mobile menu dashboard link routing.
- Delete stray `.crdownload` file.

